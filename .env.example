# Only local host port. Do not change binding to 0.0.0.0.
WEB_BIND_IP=127.0.0.1
WEB_PORT=8096
# false ONLY for localhost access through an SSH tunnel.
# After trusted HTTPS reverse proxy is configured, change to true and recreate services.
OUTREACH_SECURE_COOKIE=false
