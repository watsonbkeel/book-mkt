> **历史或阶段性记录，非当前操作指南。** 本文的版本、上限、命令、测试与部署状态只对应原记录阶段；保留原文供追溯。当前1.3.2/schema5、2000人候选池及操作方法见[部署与使用](../README_部署与使用.md)。

# 候选归档交付记录 — 2026-09-25

## 本地实现与测试

源码基点 main `025362170e473d6e2fb085a1b6cef6a2378190d2`，在原未提交修改之上连续实施，没有回退、push 或 merge。方案见 [CANDIDATE_ARCHIVAL_PLAN.md](CANDIDATE_ARCHIVAL_PLAN.md)。

完整回归：**287 passed**，24.98s；1 个 Starlette/AnyIO 依赖弃用提示。全部使用临时 SQLite、synthetic 资料和 mock，不以此声称真实邮件投递通过。`compileall`、`git diff --check` 通过。镜像在 `--network none` 下导入通过。

新测试覆盖 24 轮共 2,400 人的积压/过期/恢复研究（以配置有效期推进时钟），归档后 retention 保留原始快照，许可不变、无发送；批内 99→100 严格停止；来源刷新延长期限而普通编辑不延期；fresh 缺快照记录不立即归档；accepted/uncertain 不重写；已归档不能因审核失败或补许可被复活；重新核验失败不改记录，成功仍过现有全部资格门槛；退订不能重验复活。

## 已授权的生产更新

沿用 `http://100.92.205.125:8096` 和 `book-reader-outreach_outreach-data`，schema4 无结构迁移。更新前停止 Worker 并保存私密备份（数据库 SHA256 和 master.key 完整性已验证），重建镜像后替换 web/worker；没有删除生产卷、修改密钥或重置预算。既有自动化配置继续生效，不通过手动 send_once 或额外 IMAP 测试进行验证。此前预览环境已删除。

回退镜像：`book-reader-outreach:1.3.0-pre-candidate-archive-20260925`；私密备份位于服务器被 git 忽略的 backups 目录，不包含在源码 ZIP。回退步骤：先停止 Worker，通过 CLI pause 暂停所有自动化，备份当前数据库，恢复上述旧镜像标签后仅启动 web；保留当前数据库，不把旧备份覆盖回去。旧代码不认识 archived，不得恢复自动化，直到重新审查联系人状态及待发任务。通用数据恢复参见 UPGRADE_ROLLBACK_1.3.md。

## 真实研究与剩余边界

之前已授权研究任务 153–158 完成，新留存 10 位候选，**0 位达到可联系资格**。这不是“10 名合资格对象”的验收。实际研究中遇到的模型 persona 标签错误已由程序固定轮转标签解决，但来源证据、许可、地点、MX 等仍独立核验。

上线前统计：待核 110，现有来源有效期 14 天，最老有效基准仅约 0.7 天，过期数 0。不能为了释放名额伪造过期日期。更新后这些记录仍占名额，满额期间研究返回明确原因；实际过期后由 Worker 自动归档并恢复研究，也可通过真实来源重验解决待核项。未更改 14 天配置，未扩大或取消资格门槛。因此当前仍不能保证当天找到或发送 10 位。长期停搜缺少退出机制的问题已由本地长周期回归证明修复；生产尚未自然经过 14 天，不能宣称已完成真实长期验收。

归档保留可追溯历史快照，增长的是历史记录，活跃待核名额有界。主动匿名化仍清理原文并保留抑制哈希。
